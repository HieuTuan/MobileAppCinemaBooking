package com.cineluxe.entity;

/**
 * Enum representing the possible states of a seat during a booking lifecycle.
 * Values are lowercase to match existing database data.
 */
public enum SeatStatus {
  available, held, booked
}
